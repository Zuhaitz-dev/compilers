program Consts;
const
    N = 10;
    PI = 3.14159;
    FLAG = true;
    LETTER = 'x';
begin
    writeln(N * 2);
    writeln(PI);
    if FLAG then
        writeln('flag-true');
    writeln(LETTER);
end.
